.DS_Store
Thumbs.db
*.log

# Sources non compressees des donnees (trop grosses pour Git au-dela d'une
# petite zone -- seules les versions .gz sont commitees et deployees, voir
# tools/compress-assets.js). Copiees depuis data-prep/output/ localement.
assets/graph.json
assets/ban.json

# Graphe de connaissance du code (Graphify), usage consultatif local pour
# les sessions Claude Code -- pas un artefact de l'app, ne doit pas etre
# deploye ni publie (voir CLAUDE.md).
graphify-out/
